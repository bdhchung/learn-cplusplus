#include <iostream>
using namespace std;

int address_of_operator() {
    /**
     * The address of a variable can be obtained by preceding
     * the name of a variable with an ampersand sign (&),
     * known as address-of operator. For example:
     */
//     foo = &myvar;

}